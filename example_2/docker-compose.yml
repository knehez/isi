version: "3.3"
services:
  haproxy:
    image: "haproxytech/haproxy-alpine:2.4"
    volumes:
        - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    depends:
        - web
  web:
    build: .
    ports:
      - "5000-5004:5000"
  redis:
    image: "redis:alpine"
